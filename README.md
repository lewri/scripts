# scripts
A collection of uselessness

`corsixth-appimage.sh` - Bash script to build CorsixTH in AppImage format.\
`.github/workflows/corsixth-appimage.yml` - Conversion of the appimage bash script to an action flow (runs 2x month)\
`MPalette (folder)` - Extracted palette data from MPalette.dat
